#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

void _puts(char *str)
int main(void)
{
	_puts("Programming is like building a multilingual puzzle\n");

	return (0);
}
